#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @n: The integer for which to compute the absoulute value.
 * Returned: Absolute value of the integer.
 */

int _abs(int c)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
